
// Final ULLBC:

// Full name: std::option::Option::<u32>
#[lang_item("Option")]
pub enum Option::<u32> {
  None,
  Some(0: u32),
}

// Full name: std::prelude::v1::Some::<u32>
pub fn Some::<u32>(_1: u32) -> Option::<u32>
{
    let _0: Option::<u32>; // return
    let _1: u32; // arg #1

    bb0: {
        _0 = Option::<u32>::Some { 0: move _1 };
        return;
    }
}

// Full name: std::ops::FnOnce::call_once::<Some::<u32>, (u32,)>
pub fn call_once::<Some::<u32>, (u32,)>(_1: Some::<u32>, _2: (u32,)) -> Option::<u32>
{
    let _0: Option::<u32>; // return
    let _1: Some::<u32>; // arg #1
    let _2: (u32,); // arg #2

    bb0: {
        _0 = Some::<u32>(move _2.0) -> bb1 (unwind: bb2);
    }

    bb1: {
        return;
    }

    bb2: {
        unwind_continue;
    }
}

// Full name: std::result::Result::<u32, u8>
pub enum Result::<u32, u8> {
  Ok(0: u32),
  Err(0: u8),
}

// Full name: std::prelude::v1::Ok::<u32, u8>
pub fn Ok::<u32, u8>(_1: u32) -> Result::<u32, u8>
{
    let _0: Result::<u32, u8>; // return
    let _1: u32; // arg #1

    bb0: {
        _0 = Result::<u32, u8>::Ok { 0: move _1 };
        return;
    }
}

// Full name: std::ops::FnOnce::call_once::<Ok::<u32, u8>, (u32,)>
pub fn call_once::<Ok::<u32, u8>, (u32,)>(_1: Ok::<u32, u8>, _2: (u32,)) -> Result::<u32, u8>
{
    let _0: Result::<u32, u8>; // return
    let _1: Ok::<u32, u8>; // arg #1
    let _2: (u32,); // arg #2

    bb0: {
        _0 = Ok::<u32, u8>(move _2.0) -> bb1 (unwind: bb2);
    }

    bb1: {
        return;
    }

    bb2: {
        unwind_continue;
    }
}

// Full name: std::option::Option::<Option::<u32>>
#[lang_item("Option")]
pub enum Option::<Option::<u32>> {
  None,
  Some(0: Option::<u32>),
}

// Full name: std::option::Option::<Result::<u32, u8>>
#[lang_item("Option")]
pub enum Option::<Result::<u32, u8>> {
  None,
  Some(0: Result::<u32, u8>),
}

// Full name: std::option::Option::<T>::map::<u32, Option::<u32>, Some::<u32>>
pub fn map::<u32, Option::<u32>, Some::<u32>>(self_1: Option::<u32>, f_2: Some::<u32>) -> Option::<Option::<u32>>
{
    let _0: Option::<Option::<u32>>; // return
    let self_1: Option::<u32>; // arg #1
    let f_2: Some::<u32>; // arg #2
    let _3: isize; // anonymous local
    let x_4: u32; // local
    let _5: Option::<u32>; // anonymous local
    let _6: Some::<u32>; // anonymous local
    let _7: (u32,); // anonymous local

    bb0: {
        storage_live(_3);
        _3 = @discriminant(self_1);
        switch move _3 -> 0isize: bb1, 1isize: bb2, otherwise: bb3;
    }

    bb1: {
        _0 = Option::<Option::<u32>>::None {  };
        goto bb4;
    }

    bb2: {
        storage_live(x_4);
        x_4 = move (self_1 as variant Option::<u32>::Some).0;
        storage_live(_5);
        storage_live(_6);
        _6 = move f_2;
        storage_live(_7);
        _7 = (copy x_4,);
        _5 = call_once::<Some::<u32>, (u32,)>(move _6, move _7) -> bb5 (unwind: bb6);
    }

    bb3: {
        undefined_behavior;
    }

    bb4: {
        return;
    }

    bb5: {
        storage_dead(_7);
        storage_dead(_6);
        _0 = Option::<Option::<u32>>::Some { 0: move _5 };
        storage_dead(_5);
        storage_dead(x_4);
        goto bb4;
    }

    bb6: {
        unwind_continue;
    }
}

// Full name: std::option::Option::<T>::map::<u32, Result::<u32, u8>, Ok::<u32, u8>>
pub fn map::<u32, Result::<u32, u8>, Ok::<u32, u8>>(self_1: Option::<u32>, f_2: Ok::<u32, u8>) -> Option::<Result::<u32, u8>>
{
    let _0: Option::<Result::<u32, u8>>; // return
    let self_1: Option::<u32>; // arg #1
    let f_2: Ok::<u32, u8>; // arg #2
    let _3: isize; // anonymous local
    let x_4: u32; // local
    let _5: Result::<u32, u8>; // anonymous local
    let _6: Ok::<u32, u8>; // anonymous local
    let _7: (u32,); // anonymous local

    bb0: {
        storage_live(_3);
        _3 = @discriminant(self_1);
        switch move _3 -> 0isize: bb1, 1isize: bb2, otherwise: bb3;
    }

    bb1: {
        _0 = Option::<Result::<u32, u8>>::None {  };
        goto bb4;
    }

    bb2: {
        storage_live(x_4);
        x_4 = move (self_1 as variant Option::<u32>::Some).0;
        storage_live(_5);
        storage_live(_6);
        _6 = move f_2;
        storage_live(_7);
        _7 = (copy x_4,);
        _5 = call_once::<Ok::<u32, u8>, (u32,)>(move _6, move _7) -> bb5 (unwind: bb6);
    }

    bb3: {
        undefined_behavior;
    }

    bb4: {
        return;
    }

    bb5: {
        storage_dead(_7);
        storage_dead(_6);
        _0 = Option::<Result::<u32, u8>>::Some { 0: move _5 };
        storage_dead(_5);
        storage_dead(x_4);
        goto bb4;
    }

    bb6: {
        unwind_continue;
    }
}

// Full name: FakeTrait
trait FakeTrait

// Full name: test_crate::wrap_some
pub fn wrap_some(x_1: Option::<u32>) -> Option::<Option::<u32>>
{
    let _0: Option::<Option::<u32>>; // return
    let x_1: Option::<u32>; // arg #1
    let _2: Option::<u32>; // anonymous local

    bb0: {
        storage_live(_2);
        _2 = copy x_1;
        _0 = map::<u32, Option::<u32>, Some::<u32>>(move _2, const Some::<u32>) -> bb1 (unwind: bb2);
    }

    bb1: {
        storage_dead(_2);
        return;
    }

    bb2: {
        unwind_continue;
    }
}

// Full name: test_crate::wrap_ok
pub fn wrap_ok(x_1: Option::<u32>) -> Option::<Result::<u32, u8>>
{
    let _0: Option::<Result::<u32, u8>>; // return
    let x_1: Option::<u32>; // arg #1
    let _2: Option::<u32>; // anonymous local

    bb0: {
        storage_live(_2);
        _2 = copy x_1;
        _0 = map::<u32, Result::<u32, u8>, Ok::<u32, u8>>(move _2, const Ok::<u32, u8>) -> bb1 (unwind: bb2);
    }

    bb1: {
        storage_dead(_2);
        return;
    }

    bb2: {
        unwind_continue;
    }
}



